# Project Rules

All project conventions and AI guidance live in `.cursor/rules/`.
Read both files before making changes:
- `.cursor/rules/tradinghost.mdc` — platform conventions (deployment, env vars, secrets, logging, persistence)
- `.cursor/rules/engine.mdc` — trading engine specifics (setup, recipes, risk management)
